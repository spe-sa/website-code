virtualenv env --no-site-packages

source env/bin/activate

download the requirements.txt from github project

pip install -r requirements.txt --no-cache-dir

git clone https://github.com/spe-sa/website-code.git website

./manage.py makemigrations
./manage.py migrate

./manage.py loaddata data/auth_users.json (if localhost)

./data/JB_Data_Dump/load_all_data.sh
NOTE: change the JB_Data_Dump to whatever snapshot of data you want; in future will be a separate repository

./manage.py runserver

